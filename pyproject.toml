[project]
name = "firefly-importer"
version = "0.1.0"
description = "Imports transactions from a Starling Bank account into Firefly III"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]
